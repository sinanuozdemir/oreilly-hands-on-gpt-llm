[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300  # model download + load can take a bit on first deploy
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
